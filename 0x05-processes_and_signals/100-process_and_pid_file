#!/usr/bin/env bash
#Bash script that:
echo $$ >/var/run/myscript.pid

trap "sudo rm -f /var/run/myscript.pid" SIGQUIT
trap 'echo "I hate the kill command"' SIGTERM
trap 'echo "Y U no love me?!"' SIGINT

while ((1)); do
	echo "To infinity and beyond"
	sleep 2
done
